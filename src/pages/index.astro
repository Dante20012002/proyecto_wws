---
import Layout from '../layouts/Layout.astro';
---

<Layout title="WWS - Inicio">
  <section class="hero">
    <div class="container">
      <h1>Transformando ideas en soluciones digitales</h1>
      <p>Desarrollamos soluciones tecnológicas innovadoras para impulsar tu negocio</p>
      <div class="hero-buttons">
        <a href="/contacto" class="btn btn-primary">Contáctanos</a>
        <a href="/servicios" class="btn btn-secondary">Nuestros Servicios</a>
      </div>
    </div>
  </section>

  <section class="services">
    <div class="container">
      <h2>Nuestros Servicios</h2>
      <div class="services-grid">
        <div class="service-card">
          <img src="/icons/web-design.svg" alt="Diseño Web" />
          <h3>Diseño Web</h3>
          <p>Creamos diseños modernos y atractivos que cautivan a tus usuarios</p>
        </div>
        <div class="service-card">
          <img src="/icons/development.svg" alt="Desarrollo" />
          <h3>Desarrollo</h3>
          <p>Desarrollamos soluciones tecnológicas a medida para tu negocio</p>
        </div>
        <div class="service-card">
          <img src="/icons/marketing.svg" alt="Marketing Digital" />
          <h3>Marketing Digital</h3>
          <p>Impulsamos tu presencia online con estrategias efectivas</p>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .hero {
    padding: var(--spacing-xl) 0;
    background: var(--color-background-light);
    text-align: center;
  }

  .hero h1 {
    font-size: var(--text-4xl);
    color: var(--color-text);
    margin-bottom: var(--spacing-md);
  }

  .hero p {
    font-size: var(--text-xl);
    color: var(--color-text-muted);
    margin-bottom: var(--spacing-lg);
  }

  .hero-buttons {
    display: flex;
    gap: var(--spacing-md);
    justify-content: center;
  }

  .btn {
    padding: 1rem 2rem;
    border-radius: var(--radius-md);
    font-weight: 500;
    transition: all 0.3s ease;
  }

  .btn-primary {
    background: var(--color-primary);
    color: var(--color-text-light);
  }

  .btn-primary:hover {
    background: var(--color-secondary);
    color: var(--color-text-light);
  }

  .btn-secondary {
    background: transparent;
    border: 2px solid var(--color-primary);
    color: var(--color-primary);
  }

  .btn-secondary:hover {
    background: var(--color-primary);
    color: var(--color-text-light);
  }

  .services {
    padding: var(--spacing-xl) 0;
  }

  .services h2 {
    text-align: center;
    font-size: var(--text-3xl);
    margin-bottom: var(--spacing-xl);
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--spacing-lg);
  }

  .service-card {
    padding: var(--spacing-lg);
    background: var(--color-background);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
    text-align: center;
    transition: transform 0.3s ease;
  }

  .service-card:hover {
    transform: translateY(-5px);
  }

  .service-card img {
    width: 64px;
    height: 64px;
    margin-bottom: var(--spacing-md);
  }

  .service-card h3 {
    font-size: var(--text-xl);
    margin-bottom: var(--spacing-sm);
  }

  .service-card p {
    color: var(--color-text-muted);
  }

  @media (max-width: 768px) {
    .hero h1 {
      font-size: var(--text-3xl);
    }

    .hero p {
      font-size: var(--text-lg);
    }

    .hero-buttons {
      flex-direction: column;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
